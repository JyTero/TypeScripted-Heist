export enum BatteleMoveTarget {
    Self,
    Ally,
    Enemy,
}

export enum CharacterFaction {
    None,
    Player,
    Enemy,

}

export enum BattleMoveCategory {
    Offensive,
    Defensive,
    Utility,
    Support,
}